import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.keidson.testNovo',
  appName: 'Teste Novo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
